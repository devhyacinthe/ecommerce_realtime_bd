apiVersion: 1
datasources:
  - name: ClickHouse
    type: grafana-clickhouse-datasource
    access: proxy
    jsonData:
      host: clickhouse
      port: 9000
      username: default
      protocol: native
    secureJsonData:
      password: ${CLICKHOUSE_PASSWORD}